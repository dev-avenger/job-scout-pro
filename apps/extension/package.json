{
  "name": "@auto-job-apply/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@auto-job-apply/tsconfig": "workspace:*",
    "@types/chrome": "^0.0.270",
    "typescript": "^5.6.0"
  }
}
